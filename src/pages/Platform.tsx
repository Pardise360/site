import SubPage from "./SubPage";

const Platform = () => (
  <SubPage
    title="platform"
    description="Discover the securify platform: encryption, monitoring and access control built to keep your data safe at every layer."
  />
);

export default Platform;
